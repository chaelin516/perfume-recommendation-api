# Whiff API Server - 필수 라이브러리만

# FastAPI 및 서버 관련 (필수)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Firebase 관련 (필수)
firebase-admin==6.2.0

# 🆕 JWT 인증 관련 (추가)
PyJWT==2.8.0

# 데이터 검증 (필수)
pydantic==2.5.0

# HTTP 요청 (필수)
requests==2.31.0

# 환경변수 관리 (필수)
python-dotenv==1.0.0

# 기본 유틸리티
typing-extensions==4.8.0